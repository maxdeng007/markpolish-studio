# MarkPolish Studio - Requirements
# Core dependencies
streamlit>=1.28.0
markdown>=3.4.0
Pillow>=10.0.0
requests>=2.31.0
python-docx>=0.8.11

# Optional: AI Integration
openai>=1.0.0
httpx>=0.24.0

# Optional: PDF Generation (install at least one)
# Recommended: reportlab (pure Python, most reliable)
reportlab>=4.0.0

# Alternative PDF libraries (optional, install if needed)
# weasyprint>=60.0.0  # Requires system dependencies (libgobject, etc.)
# xhtml2pdf>=0.2.11   # May have system dependencies
# pdfkit>=1.0.0       # Requires wkhtmltopdf system binary

# Note: Standard library modules (no installation needed):
# - re, urllib.parse, base64, io, os, json, hashlib, datetime, html.parser, difflib, time

